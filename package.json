{
  "name": "rfid-project-cpid710r8-monorepo-root",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": ">=20.19.0",
    "npm": ">=11.6.0"
  },
  "scripts": {
    "install": "npm install --workspace web --ignore-scripts",
    "build": "npm run build --workspace web",
    "start": "npm run preview --workspace web -- --host 0.0.0.0 --port ${DEPLOY_RUN_PORT:-4173}"
  },
  "workspaces": [
    "web"
  ]
}
