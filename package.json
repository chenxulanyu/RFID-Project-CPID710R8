{
  "name": "rfid-project-cpid710r8-monorepo-root",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": ">=20.19.0",
    "npm": ">=11.6.0"
  },
  "scripts": {
    "install": "npm install --workspace web --ignore-scripts",
    "build": "npm run build --workspace web",
    "start": "node serve.mjs"
  },
  "workspaces": [
    "web"
  ]
}
